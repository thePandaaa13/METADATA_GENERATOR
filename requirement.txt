# =========================================================
# Content Metadata Builder Agent - requirements.txt
# Install everything at once:  pip install -r requirements.txt
# =========================================================

# ---- Core LangChain ----
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14

# ---- LLM provider: Groq (free, fast) ----
langchain-groq==0.2.3

# ---- Embeddings (free, runs locally, no API key needed) ----
langchain-huggingface==0.1.2
sentence-transformers==3.3.1

# ---- Vector database (for RAG) ----
chromadb==0.5.23

# ---- Structured data / schemas ----
pydantic==2.10.4
pydantic-settings==2.7.1

# ---- Env variables (.env file support) ----
python-dotenv==1.0.1

# ---- Document loaders: PDF, DOCX, PPTX ----
pypdf==5.1.0
python-docx==1.1.2
python-pptx==1.0.2

# ---- OCR (for scanned images / image-based content) ----
pytesseract==0.3.13
Pillow==11.1.0

# ---- Data handling + Excel export ----
pandas==2.2.3
openpyxl==3.1.5

# ---- Backend API ----
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20

# ---- Database (stores metadata rows before/alongside Excel export) ----
sqlalchemy==2.0.36

# ---- Token counting (useful for chunking text correctly) ----
tiktoken==0.8.0

# ---- Testing ----
pytest==8.3.4

streamlit==1.41.1
starlette==0.41.3